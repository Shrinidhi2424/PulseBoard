// WebSocket Server entrypoint (stub for Phase 1, fully implemented in Phase 4)
import { initialBoardState } from "./boardState.js";

console.log("[PulseBoard Server] Server stub initialized. Initial board columns:", initialBoardState.columnOrder.length);
